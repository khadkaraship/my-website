# Deploy settings for Netlify. Cloudflare Pages uses the same two values:
#   Build command:      npm run build
#   Publish directory:  dist

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"
